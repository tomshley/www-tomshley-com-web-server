apiVersion: v1
kind: Service
metadata:
  name: www-tomshley-com-web-server-k8service
spec:
  selector:
    app: www-tomshley-com-web-server
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
